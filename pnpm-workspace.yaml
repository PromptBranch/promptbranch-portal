packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  better-sqlite3: true
  esbuild: true
  sharp: false
overrides:
  # chevrotain 11.1.2 pins lodash-es 4.17.23; keep Mermaid's parser tree on
  # the patched release until it updates that exact transitive dependency.
  lodash-es: 4.18.1
  # next hard-pins postcss 8.4.31, which is under active advisories; force the
  # same safe minor already used by @tailwindcss/postcss and vite.
  postcss: 8.5.26
  # next's range (^0.34.3 || ^0.35.3) lets pnpm keep the stale 0.34.x lockfile
  # entry; pin to the patched line explicitly.
  sharp: ^0.35.3
